{
  "name": "falloutofcivilization",
  "private": true,
  "version": "0.7.1",
  "type": "module",
  "workspaces": [
    "backend",
    "client",
    "game"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n game,backend,client -c yellow,cyan,magenta \"npm run dev -w game\" \"npm run dev -w backend\" \"npm run dev -w client\"",
    "dev:game": "npm run dev -w game",
    "dev:backend": "npm run dev -w backend",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w game && npm run build -w backend && npm run build -w client",
    "test": "npm run test -w game && npm run test -w backend && npm run test -w client",
    "test:postgres": "npm run test:postgres -w backend",
    "db:migrate": "npm run db:migrate -w backend",
    "content:validate": "npm run validate -w game"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.2"
  }
}
